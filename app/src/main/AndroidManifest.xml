<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.laion.logintestv2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".RegisterMatter" />

        <activity android:name=".contacts_list" />

        <activity
            android:name=".UserContacts"
            android:label="@string/title_user_contacts"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisterOne" />
        <activity android:name=".RegisterTwo" />
        <activity android:name=".Login">
        </activity>
        <activity android:name=".RegisterThree" />
        <activity
            android:name=".User"
            android:label="@string/title_activity_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserProfile"
            android:label="Perfil"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>